# test rules kubernetes
# test tags kubernetes
# test exit-status 2

metadata:
  name: example
  labels: {}
spec:
  template:
    spec:
      containers:
        - name: test
          resources:
            limits:
              cpu: 4000m
              memory: 5Gi
            requests:
              cpu: 4000m
              memory: 5Gi

---

metadata:
  name: example
  labels: {}
spec:
  template:
    spec:
      containers:
        - name: test
          resources:
            limits:
              cpu: 200m
            requests:
              cpu: 100m

---

metadata:
  name: example
spec:
  template:
    spec:
      containers:
        - name: test
          resources:
            limits:
              cpu: 4000m
              memory: 5Gi
            requests:
              cpu: 4000m
              memory: 5Gi

