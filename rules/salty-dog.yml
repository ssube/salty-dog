rules:
  - name: salty-dog-rule
    desc: rules must be complete
    level: info
    tags:
      - important
      - salty-dog

    select: '$.rules[*]'
    check:
      type: object
      additionalProperties: false
      required:
        # metadata
        - name
        - desc
        - level
        - tags
        # data
        - select
        - check
      properties:
        name:
          type: string
        desc:
          type: string
        level:
          type: string
        tags:
          type: array
          items:
            type: string
            pattern: "[-:a-z0-9]+"
        select:
          type: string
        filter:
          type: object
        check:
          type: object